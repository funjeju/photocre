import { ko } from '@/lib/i18n/ko';

export default function ApplyPage() {
  return (
    <div className="flex flex-1 items-center justify-center">
      <p className="text-muted-foreground text-sm">{ko.apply.title} — Phase 3에서 구현 예정</p>
    </div>
  );
}
