rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Storage는 Admin SDK(서버)에서만 접근 — 클라이언트 직접 접근 전부 차단
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
